import React from 'react';

export const Button = () => {
  return (
    <button className="bg-primary text-white px-4 py-2 rounded text-secondary">
      Shared Button
    </button>
  );
};

export default Button;